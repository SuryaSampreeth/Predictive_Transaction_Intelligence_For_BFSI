# Local Development Environment
# This file is used when running `npm run dev` locally
# On Vercel, the API is served from the same domain via serverless functions

# For local development with the Python backend running on port 8000:
VITE_API_URL=http://localhost:8000

# Leave empty or comment out to use relative URLs (for Vercel deployment testing):
# VITE_API_URL=
